services:
  NetEngKit:
    build: .
    image: netengkit:latest
    container_name: NetEngKit
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    cap_add:
      - NET_RAW
    restart: unless-stopped
